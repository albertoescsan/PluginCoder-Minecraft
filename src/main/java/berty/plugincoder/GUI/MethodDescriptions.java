package berty.plugincoder.GUI;

import org.bukkit.inventory.ItemStack;

public class MethodDescriptions {
    public static ItemStack setDescription(String methodName, String classType, ItemStack itemStack){
        /*if(methodName.equals("player")){
            itemStack=PluginCoder.getCoderGUI().createItemWithDescription(itemStack,"Devuelve el jugador que realiza el evento");
        }else if(methodName.equals("text")){
            itemStack=PluginCoder.getCoderGUI().createItemWithDescription(itemStack,"Devuelve una representación en texto del objeto");
        }*/
        return itemStack;
    }
}
